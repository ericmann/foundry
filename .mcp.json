{
  "mcpServers": {
    "foundry": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"],
      "env": {
        "FOUNDRY_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}",
        "FOUNDRY_CONFIG": "${FOUNDRY_CONFIG:-}",
        "FOUNDRY_PROFILE": "${FOUNDRY_PROFILE:-}"
      }
    }
  }
}
